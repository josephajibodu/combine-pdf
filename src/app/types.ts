export type PDF = {
  title: string,
  filename: string,
  language: string,
  category: string[],
  description: string,
  author: string,
  thumbnail: string,
  updated: string,
  source: string,
};